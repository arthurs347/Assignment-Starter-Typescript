//Problem 1

const concatNumbers = (num1: number, num2: number): string => {
  return num1.toString() + num2.toString();
};

export default concatNumbers; // Uncomment this line when done writing the function
